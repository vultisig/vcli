# Vault Configuration for vcli
# Copy this file to vault.env and fill in your values
#
# vcli.sh auto-loads this file! No manual sourcing needed.
# Just run: ./local/scripts/vcli.sh <command>
#
# Search order: ./local/vault.env -> ~/.vultisig/vault.env

# Path to your vault backup file (.vult)
VAULT_PATH=/path/to/your/vault-backup.vult

# Vault decryption password (used for all TSS operations)
VAULT_PASSWORD=your-vault-password
